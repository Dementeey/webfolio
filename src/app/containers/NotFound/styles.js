export default theme => ({
  wrapper: {
    backgroundColor: theme.mainColor,
    color: theme.secondColor,
    font: {
      size: '2.2rem',
      weight: 600
    },
    textAlign: 'center',
    textTransform: 'UPPERCASE',
    padding: '50px 20px'
  }
})
